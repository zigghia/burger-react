import React from "react";
import classes from './FileNotFound.module.css';

const FileNotFound = (props) => {
	return (
		<div className={classes.f404}> 404</div>
	);
}

export default FileNotFound;
